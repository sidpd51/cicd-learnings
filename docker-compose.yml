version: "3.9"

services:
  backend:
    build: .   # 👈 build from local Dockerfile in current directory
    container_name: my-backend
    ports:
      - "3051:3001"   # 👈 host:container → map 3051 on your machine to 3000 inside container
    environment:
      - PORT=3001     # 👈 tell your app to listen on 3000 inside container
    restart: unless-stopped
